const { Student, Representative, sequelize } = require('../models');

const registerStudent = async (res, req) => {

    const t = await sequelize.transaction();

    try {
        
        const {
            //datos del representante
            cedula_repre,
            
        }

    } catch (err) {
        
    }
}